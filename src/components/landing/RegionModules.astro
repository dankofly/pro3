---
export interface Props {
  sectionTitle: string;
  sectionSubtitle: string;
  regions: Array<{ id: string; name: string; content: string }>;
  ctaLabel: string;
}

const { sectionTitle, sectionSubtitle, regions, ctaLabel } = Astro.props;
---

<section id="regionen" class="py-16 sm:py-20 bg-white">
  <div class="max-w-5xl mx-auto px-4 sm:px-6">
    <h2 class="text-2xl sm:text-3xl font-bold text-gray-900 text-center mb-3">
      {sectionTitle}
    </h2>
    <p class="text-gray-600 text-center max-w-2xl mx-auto mb-12">
      {sectionSubtitle}
    </p>

    <div class="grid sm:grid-cols-2 gap-5">
      {regions.map((region) => (
        <div class="bg-gray-50 rounded-xl p-5 sm:p-6 border border-gray-100 hover:border-sky-200 transition-colors">
          <h3 class="text-lg font-semibold text-gray-900 mb-2 flex items-center gap-2">
            <svg class="w-5 h-5 text-sky-600 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            {region.name}
          </h3>
          <p class="text-gray-600 text-sm leading-relaxed mb-3">
            {region.content}
          </p>
          <a
            href="#anfrage"
            class="inline-flex items-center text-sm font-medium text-sky-600 hover:text-sky-800 transition-colors"
          >
            {ctaLabel}
            <svg class="w-4 h-4 ml-1" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>
      ))}
    </div>
  </div>
</section>
